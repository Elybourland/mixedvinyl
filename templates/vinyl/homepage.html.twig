{% extends 'base.html.twig' %}

{% block title %}Create a New Record | {{ parent() }}{% endblock %}

{% block body %}

<h1>{{ title }}</h1>

{# TODO: add an image on the record #}

<div>
  Tracks:

  <ul>
    {% for track in tracks %}
      <li>
        {{ track.song }} - {{ track.artist }}
      </li>
    {% endfor %}
  </ul> 
</div>

{% endblock %}